const colors = {
  primary: '#0A56D9', // refined royal blue
  accent: '#0A56D9', // blue accent for CTAs (replaces red/orange)
  background: '#F3F6FB', // very light cool gray
  surface: '#FFFFFF',
  text: '#0B1B2B', // deep charcoal
  muted: '#637381',
  soft: '#EAF2FF',
  shadow: 'rgba(2,6,23,0.08)'
};

const spacing = {
  xs: 6,
  sm: 12,
  md: 16,
  lg: 24,
};

const radii = {
  sm: 6,
  md: 10,
  lg: 14,
};

export default { colors, spacing, radii };
